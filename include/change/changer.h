#pragma once

#include "change/detector.h"

namespace change {

/*
class Changer {
    
public:

    Changer(
            unsigned frameRate = 44100, // input sampling rate, Hz
            float lower = 60,           // pitch search range lower bound, Hz
            float upper = 900);         // pitch search range upper bound, Hz

    void feed(std::vector<float> &data);

    std::vector<float> pull();

private:

    Detector detector;
    
    std::deque<float> input, processed;

};
*/

}

